name: Keep Supabase Alive
on:
  schedule:
    # Jalan tiap hari jam 00:00 biar aman
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Database
        run: |
          curl --fail --silent -X GET "${{ secrets.SUPABASE_URL }}/rest/v1/posts?select=id&limit=1" \
          -H "apikey: ${{ secrets.SUPABASE_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY }}"
